# Scripts

This directory contains scripts for processing and building Markdown content.

## Examples

- `build.sh` – Converts Markdown files to HTML
- `new-post.sh` – Creates a new post from the post template
