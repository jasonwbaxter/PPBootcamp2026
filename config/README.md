# Config

This directory contains configuration files for the Markdown Content Management project.

## Examples

- `site.json` – Site-wide settings (title, description, base URL, etc.)
- `navigation.json` – Navigation menu structure
